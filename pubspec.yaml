name: cash_toss
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.4.0  # Example dependency

dev_dependencies:
  flutter_test:
    sdk: flutter  # Correctly specified under dev_dependencies

flutter:
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: Pacifico
      fonts:
        - asset: fonts/Pacifico-Regular.ttf
